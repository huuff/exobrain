# `ReplicaSet`
The building object for Kubernetes scalability and self-healing. A `ReplicaSet` manages a set number of [[pod]]s, and, in case the actual number of pods running differs from the specified one, takes actions to reconcile the state (such as starting new pods or deleting old ones). 
