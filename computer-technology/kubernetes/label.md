# Label
Metadata that serves for grouping objects together for the purposes of managing or discovering them. You can specify which labels and values you want to act on by using the `-l «label»=«value»` argument in `kubectl`
